# Casher

Minimal Vite + React + Tailwind UI skeleton for an expense tracker. Matches the visual design of a provided reference but is implemented from scratch.

## Quick start

```bash
cd casher
npm install
npm run dev
```
